TRACKWORLD DASHBOARD

1. Open Terminal in this folder.
2. Run: npm install
3. Run: npm start
4. Open: http://localhost:3000

The current version uses mock itinerary generation and does not call Gemini.
Keep your real Gemini API key only in .env. Never place it in public files.
